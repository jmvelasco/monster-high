# Tasks: Character Detail Redesign

- [x] 1. Update `GroupSelector` styles to differentiate selection (Pink) vs available (Gray) vs creation (Purple).
- [x] 2. Create a hook `useCharacterList` to fetch all characters from SWR cache. (Already exists as useCharacters)
- [x] 3. Create `FriendThumbnails` component with TDD to parse the `mejoresAmigos` string, match names, and display avatars.
- [x] 4. Update `CharacterDetail` component to include `FriendThumbnails` under the image.
- [x] 5. Update `CharacterDetail.module.css` to implement the Sticky Column layout for desktop.
